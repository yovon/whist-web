[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wd-models"
version = "0.1.0"
description = "Local WD_models package used by the wd-population-simulations project"
authors = [ { name = "Local User" } ]
readme = "README.md"
requires-python = ">=3.8"
